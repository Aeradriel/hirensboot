<%= form_for(@windows_image) do |f| %>
    <% if @windows_image.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@windows_image.errors.count, "error") %> prohibited this windows_image from being saved:</h2>

          <ul>
            <% @windows_image.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>


    <div class="panel panel-primary">
      <div class="panel-heading">Informations sur le binaire</div>
      <table class="table">
        <tr>
          <td><%= f.label :name, 'Nom' %></td>
          <td><%= f.text_field :name   , :class => "form-control"  %></td>
        </tr>
        <tr>
          <td><%= f.label :path, 'Binaire' %></td>
          <td><%= select_tag :binaries, options_for_select(@binaries), multiple: true %></td>
        </tr>
        <tr>
          <td colspan="2"><%= f.submit %></td>
        </tr>
      </table>
    </div>
<% end %>
